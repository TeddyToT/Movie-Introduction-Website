// import { create } from "zustand";

// interface TrailerState {
//   showTrailer: boolean;
//   openTrailer: () => void;
//   closeTrailer: () => void;
// }

// export const useTrailerModal = create<TrailerState>((set) => ({
//   showTrailer: false,
//   openTrailer: () => set({ showTrailer: true }),
//   closeTrailer: () => set({ showTrailer: false }),
// }));
